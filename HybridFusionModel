{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[{"sourceType":"datasetVersion","sourceId":1873742,"datasetId":1115384,"databundleVersionId":1911785}],"dockerImageVersionId":31329,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# This Python 3 environment comes with many helpful analytics libraries installed\n# It is defined by the kaggle/python Docker image: https://github.com/kaggle/docker-python\n# For example, here's several helpful packages to load\n\nimport numpy as np # linear algebra\nimport pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)\n\n# Input data files are available in the read-only \"../input/\" directory\n# For example, running this (by clicking run or pressing Shift+Enter) will list all files under the input directory\n\nimport os\nfor dirname, _, filenames in os.walk('/kaggle/input'):\n    for filename in filenames:\n        print(os.path.join(dirname, filename))\n\n# You can write up to 20GB to the current directory (/kaggle/working/) that gets preserved as output when you create a version using \"Save & Run All\" \n# You can also write temporary files to /kaggle/temp/, but they won't be saved outside of the current session","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:24:47.982579Z","iopub.execute_input":"2026-04-02T15:24:47.982966Z","iopub.status.idle":"2026-04-02T15:24:54.755472Z","shell.execute_reply.started":"2026-04-02T15:24:47.982929Z","shell.execute_reply":"2026-04-02T15:24:54.754816Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"!pip install -q timm albumentations opencv-python-headless pydicom scikit-learn einops fvcore iopath yacs termcolor\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:24:54.756432Z","iopub.execute_input":"2026-04-02T15:24:54.757216Z","iopub.status.idle":"2026-04-02T15:24:58.280842Z","shell.execute_reply.started":"2026-04-02T15:24:54.757189Z","shell.execute_reply":"2026-04-02T15:24:58.280047Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"!git clone https://github.com/MzeroMiko/VMamba.git /kaggle/working/VMamba\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:24:58.282131Z","iopub.execute_input":"2026-04-02T15:24:58.282470Z","iopub.status.idle":"2026-04-02T15:24:58.403028Z","shell.execute_reply.started":"2026-04-02T15:24:58.282443Z","shell.execute_reply":"2026-04-02T15:24:58.402244Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import os\nimport sys\nimport cv2\nimport random\nimport warnings\nimport inspect\nimport numpy as np\nimport pandas as pd\nfrom pathlib import Path\nfrom tqdm.auto import tqdm\n\nimport torch\nimport torch.nn as nn\nfrom torch.utils.data import Dataset, DataLoader, WeightedRandomSampler\n\nimport timm\nimport albumentations as A\nfrom albumentations.pytorch import ToTensorV2\n\nfrom sklearn.model_selection import StratifiedGroupKFold\nfrom sklearn.metrics import (\n    roc_auc_score,\n    accuracy_score,\n    f1_score,\n    precision_score,\n    recall_score,\n    confusion_matrix\n)\n\nwarnings.filterwarnings(\"ignore\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:24:58.404333Z","iopub.execute_input":"2026-04-02T15:24:58.404701Z","iopub.status.idle":"2026-04-02T15:25:04.250647Z","shell.execute_reply.started":"2026-04-02T15:24:58.404675Z","shell.execute_reply":"2026-04-02T15:25:04.249780Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def set_seed(seed=42):\n    random.seed(seed)\n    np.random.seed(seed)\n    torch.manual_seed(seed)\n    torch.cuda.manual_seed_all(seed)\n\nset_seed(42)\nDEVICE = \"cuda\" if torch.cuda.is_available() else \"cpu\"\nprint(\"DEVICE:\", DEVICE)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:04.251642Z","iopub.execute_input":"2026-04-02T15:25:04.252140Z","iopub.status.idle":"2026-04-02T15:25:04.498642Z","shell.execute_reply.started":"2026-04-02T15:25:04.252063Z","shell.execute_reply":"2026-04-02T15:25:04.497977Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"SEARCH_ROOT = Path(\"/kaggle/input\")\n\ndef find_csv_root():\n    targets = list(SEARCH_ROOT.rglob(\"mass_case_description_train_set.csv\"))\n    if not targets:\n        raise FileNotFoundError(\"mass_case_description_train_set.csv not found under /kaggle/input\")\n    csv_path = targets[0]\n    return csv_path.parent.parent, csv_path.parent\n\nDATA_ROOT, CSV_DIR = find_csv_root()\n\nIMG_DIR = None\nfor p in [DATA_ROOT / \"jpeg\", DATA_ROOT / \"jpg\", DATA_ROOT / \"images\", DATA_ROOT / \"image\"]:\n    if p.exists():\n        IMG_DIR = p\n        break\n\nif IMG_DIR is None:\n    for p in DATA_ROOT.rglob(\"*\"):\n        if p.is_dir() and p.name.lower() in [\"jpeg\", \"jpg\", \"images\", \"image\"]:\n            IMG_DIR = p\n            break\n\nprint(\"DATA_ROOT:\", DATA_ROOT)\nprint(\"CSV_DIR:\", CSV_DIR)\nprint(\"IMG_DIR:\", IMG_DIR)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:04.499698Z","iopub.execute_input":"2026-04-02T15:25:04.500266Z","iopub.status.idle":"2026-04-02T15:25:12.744539Z","shell.execute_reply.started":"2026-04-02T15:25:04.500239Z","shell.execute_reply":"2026-04-02T15:25:12.743771Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class CFG:\n    seed = 42\n    device = DEVICE\n\n    csv_dir = CSV_DIR\n    img_dir = IMG_DIR\n\n    img_size_full = 448\n    img_size_crop = 384\n\n    batch_size = 8\n    epochs = 30\n    lr = 2e-4\n    wd = 1e-4\n    num_workers = 2\n\n    n_splits = 5\n    fold = 0\n\n    use_amp = True\n    cnn_name = \"tf_efficientnet_b3_ns\"\n    cache_path = Path(\"/kaggle/working/resolved_paths.parquet\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:12.747347Z","iopub.execute_input":"2026-04-02T15:25:12.747972Z","iopub.status.idle":"2026-04-02T15:25:12.752855Z","shell.execute_reply.started":"2026-04-02T15:25:12.747946Z","shell.execute_reply":"2026-04-02T15:25:12.751971Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def read_one(csv_name, lesion_type):\n    x = pd.read_csv(CFG.csv_dir / csv_name)\n    x[\"lesion_type_group\"] = lesion_type\n    return x\n\nmass_train = read_one(\"mass_case_description_train_set.csv\", \"mass\")\nmass_test  = read_one(\"mass_case_description_test_set.csv\", \"mass\")\ncalc_train = read_one(\"calc_case_description_train_set.csv\", \"calc\")\ncalc_test  = read_one(\"calc_case_description_test_set.csv\", \"calc\")\n\ndf = pd.concat([mass_train, mass_test, calc_train, calc_test], ignore_index=True)\nprint(df.shape)\ndf.head()\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:12.753818Z","iopub.execute_input":"2026-04-02T15:25:12.754363Z","iopub.status.idle":"2026-04-02T15:25:12.822834Z","shell.execute_reply.started":"2026-04-02T15:25:12.754320Z","shell.execute_reply":"2026-04-02T15:25:12.821956Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"df.columns = [c.strip().lower().replace(\" \", \"_\") for c in df.columns]\ndf[\"pathology\"] = df[\"pathology\"].astype(str).str.upper().str.strip()\ndf = df[df[\"pathology\"].isin([\"MALIGNANT\", \"BENIGN\", \"BENIGN_WITHOUT_CALLBACK\"])].copy()\ndf[\"label\"] = (df[\"pathology\"] == \"MALIGNANT\").astype(int)\n\nkeep_cols = [\n    \"patient_id\",\n    \"pathology\",\n    \"label\",\n    \"image_file_path\",\n    \"cropped_image_file_path\",\n    \"lesion_type_group\"\n]\ndf = df[keep_cols].copy()\nprint(df.shape)\ndf.head()\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:12.823794Z","iopub.execute_input":"2026-04-02T15:25:12.824134Z","iopub.status.idle":"2026-04-02T15:25:12.846181Z","shell.execute_reply.started":"2026-04-02T15:25:12.824075Z","shell.execute_reply":"2026-04-02T15:25:12.845438Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"if CFG.cache_path.exists():\n    df = pd.read_parquet(CFG.cache_path)\n    print(\"Loaded cached paths:\", df.shape)\nelse:\n    IMG_EXTS = {\".jpg\", \".jpeg\", \".png\", \".bmp\"}\n\n    all_image_paths = [\n        p for p in CFG.img_dir.rglob(\"*\")\n        if p.is_file() and p.suffix.lower() in IMG_EXTS\n    ]\n    print(\"Total image files found:\", len(all_image_paths))\n\n    folder_to_first_image = {}\n    foldername_to_first_image = {}\n\n    for p in all_image_paths:\n        parent_full = str(p.parent).replace(\"\\\\\", \"/\")\n        parent_name = p.parent.name\n        if parent_full not in folder_to_first_image:\n            folder_to_first_image[parent_full] = str(p)\n        if parent_name not in foldername_to_first_image:\n            foldername_to_first_image[parent_name] = str(p)\n\n    def resolve_from_csv_path_fast(rel_path, img_root):\n        if pd.isna(rel_path):\n            return None\n        rel_path = str(rel_path).replace(\"\\\\\", \"/\").strip()\n        parts = rel_path.split(\"/\")\n        folder_parts = parts[:-1]\n        target_folder = str(img_root.joinpath(*folder_parts)).replace(\"\\\\\", \"/\")\n        if target_folder in folder_to_first_image:\n            return folder_to_first_image[target_folder]\n        last_folder = folder_parts[-1] if folder_parts else None\n        if last_folder and last_folder in foldername_to_first_image:\n            return foldername_to_first_image[last_folder]\n        return None\n\n    tqdm.pandas()\n    df[\"full_img_path\"] = df[\"image_file_path\"].progress_apply(lambda x: resolve_from_csv_path_fast(x, CFG.img_dir))\n    df[\"crop_img_path\"] = df[\"cropped_image_file_path\"].progress_apply(lambda x: resolve_from_csv_path_fast(x, CFG.img_dir))\n\n    df = df[df[\"full_img_path\"].notna()].copy().reset_index(drop=True)\n    df.to_parquet(CFG.cache_path, index=False)\n    print(\"Saved cache:\", CFG.cache_path)\n\nprint(df.shape)\nprint(\"Crop available:\", df[\"crop_img_path\"].notna().sum())\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:12.847286Z","iopub.execute_input":"2026-04-02T15:25:12.847748Z","iopub.status.idle":"2026-04-02T15:25:12.928919Z","shell.execute_reply.started":"2026-04-02T15:25:12.847726Z","shell.execute_reply":"2026-04-02T15:25:12.928234Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"sgkf = StratifiedGroupKFold(n_splits=CFG.n_splits, shuffle=True, random_state=CFG.seed)\ndf[\"fold\"] = -1\n\nfor fold, (_, val_idx) in enumerate(sgkf.split(df, y=df[\"label\"], groups=df[\"patient_id\"])):\n    df.loc[val_idx, \"fold\"] = fold\n\ntrain_df = df[df[\"fold\"] != CFG.fold].reset_index(drop=True)\nvalid_df = df[df[\"fold\"] == CFG.fold].reset_index(drop=True)\n\nprint(\"train:\", train_df.shape)\nprint(\"valid:\", valid_df.shape)\nprint(train_df[\"label\"].value_counts())\nprint(valid_df[\"label\"].value_counts())\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:12.929925Z","iopub.execute_input":"2026-04-02T15:25:12.930393Z","iopub.status.idle":"2026-04-02T15:25:13.403437Z","shell.execute_reply.started":"2026-04-02T15:25:12.930351Z","shell.execute_reply":"2026-04-02T15:25:13.402651Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"train_tfms_full = A.Compose([\n    A.Resize(CFG.img_size_full, CFG.img_size_full),\n    A.HorizontalFlip(p=0.5),\n    A.RandomBrightnessContrast(p=0.5),\n    A.ShiftScaleRotate(shift_limit=0.05, scale_limit=0.10, rotate_limit=10, border_mode=cv2.BORDER_CONSTANT, p=0.5),\n    A.GaussNoise(p=0.2),\n    A.CLAHE(p=0.3),\n    A.Normalize(mean=(0.5, 0.5, 0.5), std=(0.5, 0.5, 0.5)),\n    ToTensorV2()\n])\n\ntrain_tfms_crop = A.Compose([\n    A.Resize(CFG.img_size_crop, CFG.img_size_crop),\n    A.HorizontalFlip(p=0.5),\n    A.RandomBrightnessContrast(p=0.5),\n    A.ShiftScaleRotate(shift_limit=0.05, scale_limit=0.10, rotate_limit=10, border_mode=cv2.BORDER_CONSTANT, p=0.5),\n    A.CLAHE(p=0.3),\n    A.Normalize(mean=(0.5, 0.5, 0.5), std=(0.5, 0.5, 0.5)),\n    ToTensorV2()\n])\n\nvalid_tfms_full = A.Compose([\n    A.Resize(CFG.img_size_full, CFG.img_size_full),\n    A.Normalize(mean=(0.5, 0.5, 0.5), std=(0.5, 0.5, 0.5)),\n    ToTensorV2()\n])\n\nvalid_tfms_crop = A.Compose([\n    A.Resize(CFG.img_size_crop, CFG.img_size_crop),\n    A.Normalize(mean=(0.5, 0.5, 0.5), std=(0.5, 0.5, 0.5)),\n    ToTensorV2()\n])\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:13.404609Z","iopub.execute_input":"2026-04-02T15:25:13.404911Z","iopub.status.idle":"2026-04-02T15:25:13.421562Z","shell.execute_reply.started":"2026-04-02T15:25:13.404882Z","shell.execute_reply":"2026-04-02T15:25:13.420740Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def read_img(path):\n    img = cv2.imread(path)\n    if img is None:\n        raise ValueError(f\"Could not read image: {path}\")\n    return cv2.cvtColor(img, cv2.COLOR_BGR2RGB)\n\nclass BreastDataset(Dataset):\n    def __init__(self, df, tfm_full, tfm_crop):\n        self.df = df.reset_index(drop=True)\n        self.tfm_full = tfm_full\n        self.tfm_crop = tfm_crop\n\n    def __len__(self):\n        return len(self.df)\n\n    def __getitem__(self, idx):\n        row = self.df.iloc[idx]\n        full_img = read_img(row[\"full_img_path\"])\n        crop_path = row[\"crop_img_path\"] if pd.notna(row[\"crop_img_path\"]) else row[\"full_img_path\"]\n        crop_img = read_img(crop_path)\n\n        full_img = self.tfm_full(image=full_img)[\"image\"]\n        crop_img = self.tfm_crop(image=crop_img)[\"image\"]\n        label = torch.tensor(row[\"label\"], dtype=torch.float32)\n\n        return {\"full_img\": full_img, \"crop_img\": crop_img, \"label\": label}\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:13.422786Z","iopub.execute_input":"2026-04-02T15:25:13.423202Z","iopub.status.idle":"2026-04-02T15:25:13.429813Z","shell.execute_reply.started":"2026-04-02T15:25:13.423180Z","shell.execute_reply":"2026-04-02T15:25:13.429231Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class_counts = train_df[\"label\"].value_counts().to_dict()\nsample_weights = train_df[\"label\"].map(lambda x: 1.0 / class_counts[x]).values\nsampler = WeightedRandomSampler(sample_weights, len(sample_weights), replacement=True)\n\ntrain_ds = BreastDataset(train_df, train_tfms_full, train_tfms_crop)\nvalid_ds = BreastDataset(valid_df, valid_tfms_full, valid_tfms_crop)\n\ntrain_loader = DataLoader(train_ds, batch_size=CFG.batch_size, sampler=sampler, num_workers=CFG.num_workers, pin_memory=True, drop_last=True)\nvalid_loader = DataLoader(valid_ds, batch_size=CFG.batch_size, shuffle=False, num_workers=CFG.num_workers, pin_memory=True)\n\nbatch = next(iter(train_loader))\nprint(batch[\"full_img\"].shape, batch[\"crop_img\"].shape, batch[\"label\"].shape)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:13.430756Z","iopub.execute_input":"2026-04-02T15:25:13.431162Z","iopub.status.idle":"2026-04-02T15:25:17.264105Z","shell.execute_reply.started":"2026-04-02T15:25:13.431076Z","shell.execute_reply":"2026-04-02T15:25:17.263302Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"VMAMBA_REPO = Path(\"/kaggle/working/VMamba\")\nif VMAMBA_REPO.exists():\n    sys.path.append(str(VMAMBA_REPO))\nprint(VMAMBA_REPO, VMAMBA_REPO.exists())\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:17.265447Z","iopub.execute_input":"2026-04-02T15:25:17.265854Z","iopub.status.idle":"2026-04-02T15:25:17.271394Z","shell.execute_reply.started":"2026-04-02T15:25:17.265825Z","shell.execute_reply":"2026-04-02T15:25:17.270559Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"VSSM = None\n\ntry:\n    from classification.models.vmamba import VSSM\n    print(\"Imported: classification.models.vmamba.VSSM\")\nexcept Exception as e1:\n    try:\n        from vmamba import VSSM\n        print(\"Imported: vmamba.VSSM\")\n    except Exception as e2:\n        print(\"VSSM import failed\")\n        print(\"Error 1:\", e1)\n        print(\"Error 2:\", e2)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:17.272435Z","iopub.execute_input":"2026-04-02T15:25:17.272884Z","iopub.status.idle":"2026-04-02T15:25:17.378635Z","shell.execute_reply.started":"2026-04-02T15:25:17.272862Z","shell.execute_reply":"2026-04-02T15:25:17.377845Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class VSSMEncoder(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.using_fallback = False\n\n        if VSSM is None:\n            self.using_fallback = True\n            self.backbone = timm.create_model(\n                \"convnext_tiny\",\n                pretrained=True,\n                num_classes=0,\n                global_pool=\"avg\"\n            )\n            self.out_dim = self.backbone.num_features\n            return\n\n        try:\n            self.backbone = VSSM(\n                patch_size=4,\n                in_chans=3,\n                num_classes=0,\n                depths=[2, 2, 9, 2],\n                dims=96\n            )\n            self.out_dim = 768\n        except Exception as e:\n            print(\"Falling back to ConvNeXt-Tiny because VSSM init failed:\", e)\n            self.using_fallback = True\n            self.backbone = timm.create_model(\n                \"convnext_tiny\",\n                pretrained=True,\n                num_classes=0,\n                global_pool=\"avg\"\n            )\n            self.out_dim = self.backbone.num_features\n\n    def forward(self, x):\n        if self.using_fallback:\n            return self.backbone(x)\n\n        try:\n            feat = self.backbone(x)\n\n            if isinstance(feat, (list, tuple)):\n                feat = feat[-1]\n\n            if feat.ndim == 4:\n                feat = feat.mean(dim=(2, 3))\n            elif feat.ndim == 3:\n                feat = feat.mean(dim=1)\n\n            return feat\n        except Exception as e:\n            if \"selective_scan_backend\" in str(e) or isinstance(e, AssertionError):\n                if not self.using_fallback:\n                    print(\"VSSM forward failed, switching to ConvNeXt-Tiny fallback:\", e)\n                    self.using_fallback = True\n                    self.backbone = timm.create_model(\n                        \"convnext_tiny\",\n                        pretrained=True,\n                        num_classes=0,\n                        global_pool=\"avg\"\n                    ).to(x.device)\n                    self.out_dim = self.backbone.num_features\n                    return self.backbone(x)\n            raise e\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:17.379672Z","iopub.execute_input":"2026-04-02T15:25:17.380262Z","iopub.status.idle":"2026-04-02T15:25:17.388852Z","shell.execute_reply.started":"2026-04-02T15:25:17.380239Z","shell.execute_reply":"2026-04-02T15:25:17.388109Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"x = torch.randn(2, 3, CFG.img_size_crop, CFG.img_size_crop).to(CFG.device)\nm = VSSMEncoder().to(CFG.device)\n\nwith torch.no_grad():\n    y = m(x)\n\nprint(\"Encoder output shape:\", y.shape)\nprint(\"Using fallback:\", m.using_fallback)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:17.389924Z","iopub.execute_input":"2026-04-02T15:25:17.390290Z","iopub.status.idle":"2026-04-02T15:25:20.907389Z","shell.execute_reply.started":"2026-04-02T15:25:17.390268Z","shell.execute_reply":"2026-04-02T15:25:20.906629Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class HybridFusionModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n\n        self.cnn = timm.create_model(CFG.cnn_name, pretrained=True, num_classes=0, global_pool=\"avg\")\n        cnn_dim = self.cnn.num_features\n\n        self.vssm = VSSMEncoder()\n        vssm_dim = self.vssm.out_dim\n\n        self.cnn_proj = nn.Sequential(\n            nn.Linear(cnn_dim, 512),\n            nn.LayerNorm(512),\n            nn.GELU(),\n            nn.Dropout(0.2),\n        )\n\n        self.vssm_proj = nn.Sequential(\n            nn.Linear(vssm_dim, 512),\n            nn.LayerNorm(512),\n            nn.GELU(),\n            nn.Dropout(0.2),\n        )\n\n        self.gate = nn.Sequential(\n            nn.Linear(1024, 256),\n            nn.GELU(),\n            nn.Dropout(0.2),\n            nn.Linear(256, 2)\n        )\n\n        self.head = nn.Sequential(\n            nn.Linear(512, 256),\n            nn.GELU(),\n            nn.Dropout(0.3),\n            nn.Linear(256, 1)\n        )\n\n    def forward(self, full_img, crop_img):\n        f1 = self.cnn(full_img)\n        f2 = self.vssm(crop_img)\n\n        f1 = self.cnn_proj(f1)\n        f2 = self.vssm_proj(f2)\n\n        gate = torch.softmax(self.gate(torch.cat([f1, f2], dim=1)), dim=1)\n        fused = gate[:, 0:1] * f1 + gate[:, 1:2] * f2\n\n        return self.head(fused).squeeze(1)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:20.908443Z","iopub.execute_input":"2026-04-02T15:25:20.908775Z","iopub.status.idle":"2026-04-02T15:25:20.916248Z","shell.execute_reply.started":"2026-04-02T15:25:20.908740Z","shell.execute_reply":"2026-04-02T15:25:20.915394Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"pos = train_df[\"label\"].sum()\nneg = len(train_df) - pos\npos_weight = torch.tensor([neg / max(pos, 1)], device=CFG.device)\n\nmodel = HybridFusionModel().to(CFG.device)\ncriterion = nn.BCEWithLogitsLoss(pos_weight=pos_weight)\noptimizer = torch.optim.AdamW(model.parameters(), lr=CFG.lr, weight_decay=CFG.wd)\nscheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=CFG.epochs)\nscaler = torch.cuda.amp.GradScaler(enabled=CFG.use_amp)\n\nprint(\"Model ready\")\nprint(\"VSSM fallback active:\", model.vssm.using_fallback)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:20.917222Z","iopub.execute_input":"2026-04-02T15:25:20.917517Z","iopub.status.idle":"2026-04-02T15:25:22.277408Z","shell.execute_reply.started":"2026-04-02T15:25:20.917486Z","shell.execute_reply":"2026-04-02T15:25:22.276475Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"@torch.no_grad()\ndef validate(model, loader):\n    model.eval()\n    all_labels, all_probs = [], []\n\n    for batch in loader:\n        full_img = batch[\"full_img\"].to(CFG.device)\n        crop_img = batch[\"crop_img\"].to(CFG.device)\n        labels = batch[\"label\"].to(CFG.device)\n\n        with torch.cuda.amp.autocast(enabled=CFG.use_amp):\n            logits = model(full_img, crop_img)\n\n        probs = torch.sigmoid(logits)\n        all_labels.extend(labels.cpu().numpy())\n        all_probs.extend(probs.cpu().numpy())\n\n    all_labels = np.array(all_labels)\n    all_probs = np.array(all_probs)\n    preds = (all_probs >= 0.5).astype(int)\n\n    return {\n        \"auc\": roc_auc_score(all_labels, all_probs),\n        \"acc\": accuracy_score(all_labels, preds),\n        \"precision\": precision_score(all_labels, preds, zero_division=0),\n        \"recall\": recall_score(all_labels, preds, zero_division=0),\n        \"f1\": f1_score(all_labels, preds, zero_division=0),\n        \"cm\": confusion_matrix(all_labels, preds)\n    }\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:22.278534Z","iopub.execute_input":"2026-04-02T15:25:22.278910Z","iopub.status.idle":"2026-04-02T15:25:22.285302Z","shell.execute_reply.started":"2026-04-02T15:25:22.278887Z","shell.execute_reply":"2026-04-02T15:25:22.284553Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"best_auc = -1\nbest_path = \"/kaggle/working/best_hybrid_vssm_effb3.pth\"\n\nfor epoch in range(CFG.epochs):\n    model.train()\n    running_loss = 0.0\n\n    pbar = tqdm(train_loader, total=len(train_loader))\n    for batch in pbar:\n        full_img = batch[\"full_img\"].to(CFG.device)\n        crop_img = batch[\"crop_img\"].to(CFG.device)\n        labels = batch[\"label\"].to(CFG.device)\n\n        optimizer.zero_grad()\n\n        with torch.cuda.amp.autocast(enabled=CFG.use_amp):\n            logits = model(full_img, crop_img)\n            loss = criterion(logits, labels)\n\n        scaler.scale(loss).backward()\n        scaler.step(optimizer)\n        scaler.update()\n\n        running_loss += loss.item()\n        pbar.set_description(f\"Epoch {epoch+1}/{CFG.epochs} Loss {running_loss/(pbar.n+1):.4f}\")\n\n    scheduler.step()\n    metrics = validate(model, valid_loader)\n\n    print(f\"Epoch {epoch+1}: AUC={metrics['auc']:.4f} ACC={metrics['acc']:.4f} F1={metrics['f1']:.4f}\")\n\n    if metrics[\"auc\"] > best_auc:\n        best_auc = metrics[\"auc\"]\n        torch.save(model.state_dict(), best_path)\n        print(\"Saved best model\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T15:25:22.286285Z","iopub.execute_input":"2026-04-02T15:25:22.286675Z","iopub.status.idle":"2026-04-02T18:38:31.755608Z","shell.execute_reply.started":"2026-04-02T15:25:22.286653Z","shell.execute_reply":"2026-04-02T18:38:31.754641Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"model.load_state_dict(torch.load(best_path, map_location=CFG.device))\nmetrics = validate(model, valid_loader)\n\nprint(\"Best Fold Metrics\")\nprint(\"AUC      :\", round(metrics[\"auc\"], 4))\nprint(\"ACC      :\", round(metrics[\"acc\"], 4))\nprint(\"Precision:\", round(metrics[\"precision\"], 4))\nprint(\"Recall   :\", round(metrics[\"recall\"], 4))\nprint(\"F1       :\", round(metrics[\"f1\"], 4))\nprint(\"Confusion Matrix:\\n\", metrics[\"cm\"])\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T18:38:31.757541Z","iopub.execute_input":"2026-04-02T18:38:31.757799Z","iopub.status.idle":"2026-04-02T18:39:43.302049Z","shell.execute_reply.started":"2026-04-02T18:38:31.757764Z","shell.execute_reply":"2026-04-02T18:39:43.300841Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"@torch.no_grad()\ndef validate_tta(model, df_valid, n_tta=2):\n    model.eval()\n    probs_all, labels = [], None\n\n    for t in range(n_tta):\n        tfm_full = A.Compose([\n            A.Resize(CFG.img_size_full, CFG.img_size_full),\n            A.HorizontalFlip(p=1.0 if t == 1 else 0.0),\n            A.Normalize(mean=(0.5,0.5,0.5), std=(0.5,0.5,0.5)),\n            ToTensorV2()\n        ])\n        tfm_crop = A.Compose([\n            A.Resize(CFG.img_size_crop, CFG.img_size_crop),\n            A.HorizontalFlip(p=1.0 if t == 1 else 0.0),\n            A.Normalize(mean=(0.5,0.5,0.5), std=(0.5,0.5,0.5)),\n            ToTensorV2()\n        ])\n\n        ds = BreastDataset(df_valid, tfm_full, tfm_crop)\n        loader = DataLoader(ds, batch_size=CFG.batch_size, shuffle=False, num_workers=CFG.num_workers, pin_memory=True)\n\n        fold_probs, fold_labels = [], []\n        for batch in loader:\n            full_img = batch[\"full_img\"].to(CFG.device)\n            crop_img = batch[\"crop_img\"].to(CFG.device)\n            y = batch[\"label\"].to(CFG.device)\n\n            with torch.cuda.amp.autocast(enabled=CFG.use_amp):\n                logits = model(full_img, crop_img)\n\n            fold_probs.extend(torch.sigmoid(logits).cpu().numpy())\n            fold_labels.extend(y.cpu().numpy())\n\n        probs_all.append(np.array(fold_probs))\n        labels = np.array(fold_labels)\n\n    probs_mean = np.mean(probs_all, axis=0)\n    preds = (probs_mean >= 0.5).astype(int)\n\n    print(\"TTA AUC:\", round(roc_auc_score(labels, probs_mean), 4))\n    print(\"TTA ACC:\", round(accuracy_score(labels, preds), 4))\n    print(\"TTA F1 :\", round(f1_score(labels, preds, zero_division=0), 4))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T18:39:43.303847Z","iopub.execute_input":"2026-04-02T18:39:43.304788Z","iopub.status.idle":"2026-04-02T18:39:43.316513Z","shell.execute_reply.started":"2026-04-02T18:39:43.304748Z","shell.execute_reply":"2026-04-02T18:39:43.315727Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"validate_tta(model, valid_df, n_tta=2)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-02T18:39:43.317469Z","iopub.execute_input":"2026-04-02T18:39:43.317859Z","iopub.status.idle":"2026-04-02T18:42:09.227050Z","shell.execute_reply.started":"2026-04-02T18:39:43.317825Z","shell.execute_reply":"2026-04-02T18:42:09.226292Z"}},"outputs":[],"execution_count":null}]}